const RELEASE_MAJOR = 'major';
const RELEASE_MINOR = 'minor';
const RELEASE_PATCH = 'patch';

const releases = [
  RELEASE_MAJOR,
  RELEASE_MINOR,
  RELEASE_PATCH
];

module.exports = {
  RELEASE_MAJOR,
  RELEASE_MINOR,
  RELEASE_PATCH,
  releases
};
